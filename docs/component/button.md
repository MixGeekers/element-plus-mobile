---
title: 按钮
lang: zh-CN
---

# 按钮

常用按钮。

## 基本用法

:::demo 使用 `type`、`plain`、`round`、`dashed` 和 `circle` 定义 Button 的样式。

button/basic

:::

## 禁用按钮

`disabled` 属性确定按钮是否被禁用。

:::demo 使用 `disabled` 属性来确定按钮是否被禁用。它接受 `Boolean` 值。

button/disabled

:::

## 链接按钮

:::warning

`type="text"` 已被 **弃用**，并且 **将** 在 ^(3.0.0) 中删除，请考虑切换到新的 API。

^(2.2.1)中添加了新的API `link`，您可以使用`type` API设置链接按钮的主题

:::

:::demo

button/link

:::

## 文本按钮

:::tip

文字按钮自 <el-tag round effect="plain" size="small">2.2.0</el-tag> 开始升级全新设计，如果您想使用
以前版本的按钮，您可能需要查看[链接](./link.md#basic)。

API 也更新了，因为 `type` 属性也代表按钮的样式。所以我们必须制作一个新的API
`text: boolean` 用于文本按钮。

:::

没有边框和背景的按钮。

:::demo

button/text

:::

## 图标按钮

使用图标为按钮添加更多含义。您可以单独使用图标来节省一些空间，也可以与文本一起使用。

:::demo 使用 `icon` 属性添加图标。您可以在Element Plus图标组件中找到图标列表。使用 `<i>` 标签可以将图标添加到文本右侧。也可以使用自定义图标。

button/icon

:::

## 按钮组

显示为按钮组，可用于对一系列类似的操作进行分组。

在 ^(2.11.9) 中，您可以使用 `direction` 属性。

:::demo 使用标签 `<el-button-group>` 对按钮进行分组。

button/group

:::

## 加载按钮

点击按钮加载数据，按钮显示加载状态。

将 `loading` 属性设置为 `true` 以显示加载状态。

:::tip

您可以使用 `loading` 插槽或 `loadingIcon` 自定义您的加载组件

ps: `loading` slot 的优先级高于loadingIcon

:::

:::demo

button/loading

:::

## 尺寸

除了默认大小外，Button 组件还提供了三种额外的大小供您在不同的场景中进行选择。

:::demo 使用属性 `size` 通过 `large`、`small` 设置附加尺寸。

button/size

:::

## 标签 ^(2.3.4)

您可以自定义元素标签，例如button、div、a、router-link、nuxt-link。

:::demo

button/tag

:::

## 自定义颜色 ^（测试版）

您可以自定义按钮颜色。

我们将自动计算悬停颜色和活动颜色。

:::demo

button/custom

:::

## 按钮API

### 按钮属性

| 名称           | 描述                                                                    | 类型                                                                                                         | 默认     |
| -------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------- |
| 尺寸           | 按钮尺寸                                                                | ^[枚举]`'large' \| 'default' \| 'small'`                                                                     | —        |
| 类型           | 按钮类型，设置`color`时，以后者为准                                     | ^[枚举]`'default' \| 'primary' \| 'success' \| 'warning' \| 'danger' \| 'info' \| '' \| 'text' (deprecated)` | —        |
| 普通           | 判断是否为普通按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 文本 ^(2.2.0)  | 判断是否为文本按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 背景 ^(2.2.0)  | 判断文本按钮背景色是否常亮                                              | ^[布尔值]                                                                                                    | 假       |
| 链接 ^(2.2.1)  | 判断是否是链接按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 圆形           | 判断是否为圆形按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 圆             | 判断是否为圆形按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 虚线 ^(2.13.3) | 判断是否为虚线按钮                                                      | ^[布尔值]                                                                                                    | 假       |
| 加载           | 判断是否正在加载                                                        | ^[布尔值]                                                                                                    | 假       |
| 加载图标       | 自定义加载图标组件                                                      | ^[字符串] / ^[组件]                                                                                          | 正在加载 |
| 已禁用         | 禁用按钮                                                                | ^[布尔值]                                                                                                    | 假       |
| 图标           | 图标组件                                                                | ^[字符串] / ^[组件]                                                                                          | —        |
| 自动对焦       | 与原生按钮`autofocus`相同                                               | ^[布尔值]                                                                                                    | 假       |
| 原生型         | 与原生按钮的`type` 相同                                                 | ^[枚举]`'button' \| 'submit' \| 'reset'`                                                                     | 按钮     |
| 自动插入空格   | 自动在两个汉字之间插入空格（仅当文本长度为2且所有字符均为中文时才生效） | ^[布尔值]                                                                                                    | 假       |
| 颜色           | 自定义按钮颜色，自动计算`hover`和`active`颜色                           | ^[字符串]                                                                                                    | —        |
| 黑暗           | 深色模式，自动将 `color` 转换为深色模式颜色                             | ^[布尔值]                                                                                                    | 假       |
| 标签 ^(2.3.4)  | 自定义元素标签                                                          | ^[字符串] / ^[组件]                                                                                          | 按钮     |

### 按钮槽

|名称 |描述 |
| -------- | ------------------------ | |
|默认 |自定义默认内容 |
|加载|自定义加载组件 |
|图标|自定义图标组件 |

### 按钮暴露

| 名称         | 描述           | 类型                                                                                                         |
| ------------ | -------------- | ------------------------------------------------------------------------------------------------------------ |
| 参考         | 按钮 html 元素 | ^[对象]`Ref<HTMLButtonElement>`                                                                              |
| 尺寸         | 按钮尺寸       | ^[对象]`ComputedRef<'' \| 'small' \| 'default' \| 'large'>`                                                  |
| 类型         | 按钮类型       | ^[对象]`ComputedRef<'' \| 'default' \| 'primary' \| 'success' \| 'warning' \| 'info' \| 'danger' \| 'text'>` |
| 已禁用       | 按钮已禁用     | ^[对象]`ComputedRef<boolean>`                                                                                |
| 应该添加空格 | 是否加空格     | ^[对象]`ComputedRef<boolean>`                                                                                |

## 按钮组 API

### ButtonGroup 属性

| 名称           | 描述                     | 类型                                                               | 默认 |
| -------------- | ------------------------ | ------------------------------------------------------------------ | ---- |
| 尺寸           | 控制此按钮组中按钮的大小 | ^[枚举]`'large' \| 'default' \| 'small'`                           | —    |
| 类型           | 控制此按钮组中按钮的类型 | ^[枚举]`'primary' \| 'success' \| 'warning' \| 'danger' \| 'info'` | —    |
| 方向 ^(2.11.9) | 显示方向                 | ^[枚举]`'horizontal' \| 'vertical'`                                | 水平 |

### ButtonGroup 插槽

| 名称 | 描述             | 子标签 |
| ---- | ---------------- | ------ |
| 默认 | 自定义按钮组内容 | 按钮   |
