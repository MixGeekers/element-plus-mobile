---
title: 时间轴
lang: zh-CN
---

# 时间轴

直观地显示时间线。

## 基本用法

时间线可以分为多个活动。时间戳是区别于其他组件的重要特征。请注意与步骤的区别。

:::demo

timeline/basic

:::

## 模式 ^(2.13.1)

使用`mode`控制时间线和内容的相对位置。

:::tip

在 ^(2.13.1) 之后，`el-timeline` 显式设置填充样式。如果您在项目中覆盖了 `ul` 标签的填充样式，请检查以确保布局正确。

:::

:::demo

timeline/mode

:::

## 自定义节点

大小、颜色和图标可以在节点中自定义。

:::demo

timeline/custom-node

:::

## 自定义时间戳

当内容太高时，可以将时间戳放置在内容之上。

:::demo

timeline/custom-timestamp

:::

## 垂直居中

时间轴项目垂直居中。

:::demo

timeline/center

:::

## 反转 ^(2.11.9)

使用reverse属性来控制节点的顺序。

:::demo

timeline/reverse

:::

## 时间轴 API

### 时间轴属性

| 名称           | 描述                   | 类型                                                            | 默认 |
| -------------- | ---------------------- | --------------------------------------------------------------- | ---- |
| 反转 ^(2.11.9) | 是否倒序               | ^[布尔值]                                                       | 假   |
| 模式 ^(2.13.1) | 时间线和内容的相对位置 | ^[枚举]`'start' \| 'alternate' \| 'alternate-reverse' \| 'end'` | 开始 |

### 时间线时段

| 名称 | 描述                   | 子标签     |
| ---- | ---------------------- | ---------- |
| 默认 | 自定义时间线的默认内容 | 时间线项目 |

## 时间轴项目 API

### 时间线-项目属性

|名称 |描述 |类型 |默认 |
| -------------- | ------------------------ | | ------------------------------------------------------------------ | -------- |
|时间戳|时间戳内容 | ^[字符串] | ''|
|隐藏时间戳 |是否显示时间戳 | ^[布尔值] |假 |
|中心 |是否垂直居中| ^[布尔值] |假 |
|安置|时间戳的位置 | ^[枚举]`'top' \| 'bottom'` |底部|
|类型 |节点类型| ^[枚举]`'primary' \| 'success' \| 'warning' \| 'danger' \| 'info'` | ''|
|颜色 |节点的背景颜色| ^[字符串] | ''|
|尺寸|节点大小 | ^[枚举]`'normal' \| 'large'` |正常 |
|图标|图标组件 | ^[字符串] / ^[组件] | — |
|空心|图标是空心的 | ^[布尔值] |假 |

### 时间线-物品槽位

| 名称 | 描述                         |
| ---- | ---------------------------- |
| 默认 | 自定义时间线项目的默认内容   |
| 点   | 为时间线项目自定义定义的节点 |
